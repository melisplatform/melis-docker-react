# Melis Platform — run the PRE-BUILT image (no build on your machine).
#   1) cp .env.example .env
#   2) docker compose up -d
#   3) open http://localhost:8080 and follow the Melis web installer
#
# The image already ships a ready-to-run Melis project; you only add a database.
services:
  php:
    image: ${MELIS_IMAGE:-melisplatform/melis-docker:latest}
    # To build locally instead of pulling the published image, uncomment:
    # build:
    #   context: .
    #   dockerfile: ./Dockerfile
    #   args:
    #     APP_NAME: ${APP_NAME:-melis}
    #     TZ: ${TZ:-Europe/Paris}
    #     PHP_VERSION: ${PHP_VERSION:-8.3}   # 8.3 (default) | 8.4 (experimental). 8.5 not usable yet.
    #     WITH_REACT: ${WITH_REACT:-1}       # bake the React back-office (/melis-react)
    #   secrets: [github_token]              # + uncomment the top-level secrets: block
    #                                        #   below; needed or the React step hits
    #                                        #   the 60 req/h GitHub API limit.
    container_name: ${MELIS_CONTAINER_NAME:-melis}-php
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${HOST_PORT:-8080}:80"
    # Named volume seeded from the image on first run (keeps the baked code) and
    # persists what the web installer writes (config/, cache/, media). Do NOT bind
    # a host folder here or it would hide the pre-built project inside the image.
    volumes:
      - melis_app:/var/www/${APP_NAME:-melis}
      # conf/ mounted rather than baked, so tuning PHP (memory_limit, error logging)
      # or the installer guard doesn't require rebuilding the pre-built image.
      # PHP_INI_SCAN_DIR below makes PHP read conf/php.ini from here *after* the
      # baked conf.d, so it wins. Mount the DIRECTORY, never the single .ini file:
      # editors replace the file's inode, which silently strips a single-file bind
      # mount and then blocks the container from restarting at all.
      - ./conf:/melis-conf:ro
    environment:
      APP_NAME: ${APP_NAME:-melis}
      TZ: ${TZ:-Europe/Paris}
      # Read the baked conf.d first, then the mounted conf/ (later wins).
      PHP_INI_SCAN_DIR: "/usr/local/etc/php/conf.d:/melis-conf"
      HOST_PORT: ${HOST_PORT:-8080}
      # NOTE: host without :port — keep it clean (a "host:port" value breaks flyway/JDBC)
      MYSQL_HOST: ${MELIS_CONTAINER_NAME:-melis}-db
      MYSQL_HOSTNAME: ${MELIS_CONTAINER_NAME:-melis}-db
      MYSQL_DATABASE: ${MYSQL_DATABASE:-melis}
      MYSQL_USER: ${MYSQL_USER:-melis}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-melis}
      # Lifts Composer off the 60/hour unauthenticated GitHub API limit that
      # otherwise breaks the web installer's module step — and, in this repo, the
      # React enablement (3 more GitHub vcs repos). See .env.example.
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      # React back-office (/melis-react): baked into the image; this only gates the
      # entrypoint's defensive re-enable (e.g. an app volume from the old image).
      WITH_REACT: ${WITH_REACT:-1}
      MELIS_PLATFORM: ${MELIS_PLATFORM:-local}
      MELIS_MODULE: ${MELIS_MODULE:-MySite}
    networks:
      - melis
    restart: unless-stopped

  db:
    image: mysql:${MYSQL_VERSION:-8.4}
    container_name: ${MELIS_CONTAINER_NAME:-melis}-db
    # Bound to localhost only — the DB is reachable container-to-container over the
    # melis network; this host mapping is just for a local GUI client, not the LAN.
    ports:
      - "127.0.0.1:${DB_HOST_PORT:-33061}:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD:-melis}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-melis}
      MYSQL_USER: ${MYSQL_USER:-melis}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-melis}
    command:
      - "mysqld"
      - "--character-set-server=utf8mb4"
      - "--collation-server=utf8mb4_general_ci"
    volumes:
      - melis_db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u${MYSQL_USER:-melis}", "-p${MYSQL_PASSWORD:-melis}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - melis
    restart: always

# Build-time only, for the commented-out build: block above (this stack pulls the
# published image by default, so nothing here is used unless you build locally).
# secrets:
#   github_token:
#     environment: GITHUB_TOKEN

networks:
  melis:
    name: ${MELIS_CONTAINER_NAME:-melis}-network

volumes:
  melis_app:
    name: ${MELIS_CONTAINER_NAME:-melis}-app
  melis_db_data:
    name: ${MELIS_CONTAINER_NAME:-melis}-db-data
